{
  "version": "2.0.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Client",
      "request": "launch",
      "command": "yarn run start:dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}